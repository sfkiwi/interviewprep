describe('pascalsTriangle', function() {
    it('factorial function should work', function() {
        var result = factorial(5);
        expect(result).to.equal.to(120);
        var result = factorial(4);
        expect(result).to.equal.to(24);
        var result = factorial(3);
        expect(result).to.equal.to(6);
        var result = factorial(1);
        expect(result).to.equal.to(1);
    });

    xit('rockPaperScissors EXTRA CREDIT should return results for rockPaperScissors(2)', function() {
    });

    xit('rockPaperScissors EXTRA CREDIT should return results for rockPaperScissors(1)', function() {
    });

    xit('rockPaperScissors EXTRA CREDIT should return results for rockPaperScissors(5)', function() {
    });
});